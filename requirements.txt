# Model environment (Python 3.10): training, inference, projector code. Versions are the ones we ran.
# The LLaVA/BTB3D stack is not on PyPI; install it separately (see README, section 1).
# deepspeed is needed for training only; inference does not import it.
# The exact snapshot of this environment is environment/model-py310.freeze.txt.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1+cu121
torchvision==0.19.1+cu121
transformers==4.46.2
peft==0.14.0
accelerate==1.0.0
deepspeed==0.15.4  # training only
safetensors==0.4.5
sentencepiece==0.2.1
huggingface-hub==0.26.2
einops==0.8.0
einops-exts==0.0.4
numpy==1.24.4
pandas==2.3.3
tqdm==4.66.5
pillow==10.4.0
av==13.1.0
